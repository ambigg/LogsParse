//here gets the info from the analizer and just returned to the user pretty
//Basically just set the gest for every request that could get

//pub struct LogStat {
//     pub total_requests:usize,
//     pub status_codes:HashMap<u32,usize>,
//     pub top_ips: Vec<(String,usize)>,
//     pub top_urls:Vec<(String,usize)>,
//     pub methods:HashMap<String,usize>,
//     pub total_bytes:u64,
//     pub lineserrors: Vec<LogEntry>,
// }
use crate::LogStat;

pub fn clean(log_stat:&LogStat,filter:String)->String{

let x= String::new();

    x

}
